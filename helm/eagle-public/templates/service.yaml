apiVersion: v1
kind: Service
metadata:
  name: {{ include "eagle-public.fullname" . }}
  labels:
    {{- include "eagle-public.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eagle-public.selectorLabels" . | nindent 4 }}
